// Get your own free key at https://openrouteservice.org/dev/#/signup
// Put it in a local .env file as EXPO_PUBLIC_ORS_API_KEY=... (see .env.example)
// — never hardcode a real key here, it would end up committed to git.
export const ORS_API_KEY = process.env.EXPO_PUBLIC_ORS_API_KEY || 'YOUR_OPENROUTESERVICE_API_KEY';
